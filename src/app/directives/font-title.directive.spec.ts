import { FontTitleDirective } from './font-title.directive';

describe('FontTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new FontTitleDirective();
    expect(directive).toBeTruthy();
  });
});
